enum EFormState {
  initial,
  loadingForm,
  submittingForm,
  submittingFailed,
  submittingSuccess,
  success,
  failure,
}

enum EListState {
  initial,
  loading,
  loadingMore,
  refreshing,
  empty,
  loaded,
  error,
}

enum EViewState { initial, loading, loaded, error }
